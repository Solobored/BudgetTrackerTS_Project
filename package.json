{
  "name": "budgettracker-ts",
  "version": "1.0.0",
  "description": "BudgetTrackerTS - TypeScript module with server and Jest tests",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "server": "npx http-server . -p 8080 -c-1",
    "test": "jest --config jest.config.cjs --runInBand"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "ts-jest": "^29.1.0",
    "jest": "^29.5.0",
    "@types/jest": "^29.5.2",
    "http-server": "^14.1.1"
  }
}